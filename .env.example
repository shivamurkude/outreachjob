# =============================================================================
# FINDMYJOB v2 – copy to .env and fill values. See docs/SETUP.md for details.
# For staging/production use: .env.staging and .env.production as templates.
# =============================================================================

# App
ENV=development
DEBUG=true
# Min 32 characters; used for session signing and (if TOKEN_ENCRYPTION_KEY empty) token encryption
SECRET_KEY=change-me-in-production-min-32-chars

# MongoDB (required)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=findmyjob

# Redis (required for ARQ worker and future rate limits)
REDIS_URL=redis://localhost:6379/0

# Google OAuth (required for auth and Gmail)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Must match redirect URI in Google Cloud Console; backend URL for OAuth callback
GMAIL_OAUTH_REDIRECT_URI=http://localhost:8000/v1/gmail/oauth/callback

# Optional: Fernet key (base64). If empty, key is derived from SECRET_KEY for dev
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOKEN_ENCRYPTION_KEY=

# Razorpay (for payments)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
# Webhook signing secret from Razorpay dashboard
RAZORPAY_WEBHOOK_SECRET=

# OpenAI (optional; for richer resume analysis and template generation)
OPENAI_API_KEY=

# Storage: "local" for dev (files under STORAGE_LOCAL_PATH), "gcs" for prod
STORAGE_BACKEND=local
STORAGE_LOCAL_PATH=./uploads
# GCS_BUCKET_NAME=your-bucket  # when STORAGE_BACKEND=gcs

# Sentry (optional; set to enable error reporting)
SENTRY_DSN=

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
